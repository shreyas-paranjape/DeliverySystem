#%RAML 0.8

title: Goa amigo API
version: "0.1"
baseUri: "https://api.foodamigo.co.in/{version}"
mediaType: "application/json"

securitySchemes:
securedBy:

# Catalogue
/catalogue
  get:
    description: |
      Returns all the product categories and associated product info.
      Optional queryParameter can specify the parent category to start at
    queryParameters:
      root:
        type: string
        description: |
          The category to start at. All sub categoies and associated products of this category will
          be fetched.
        required: false
    responses:
    example:


# Product
/product
  get:

  post:
    description: "Creates a new product in the system"
    body:
      application/json:
        schema: |
        example: |
          {
            
          }


  /{product_id}

   get:

   put:

   delete:

# Party
/party
  get:
    description: |
      Returns all Customers/Vendors in the system.
    queryParameters:
      party_type:
        type: string
        description: Vendors/Customers/All
        default: all
      count:
        type: integer
        description: Number of enteries to retrieve
        default: 20
      start_id:
        type: integer
        description: Id at which to start the retrieval.
        default: *first entry in the party table
    responses:
      200:
        body:
          schema: |
            {
              "": "http://json-schema.org/draft-03/schema",
              "type": "object",
              "properties": {

                }
            }
          example: |

  post:
    description: |
      Creates a new Customer/Vendor in the system.
    body:
      application/json:
        schema: |
        example: |
            {
              "name": "",
              "address":
                [
                  {
                    "name": "",
                    "address_string": "",
                    "latitude": "",
                    "longitude": ""
                  }
                ]
              "comm":
                [
                  {
                    "name": "",
                    "email": "",
                    "mobile": ""
                  }
                ],
              "description": "",
              "image_url": "",
              "sites":
                [
                  {
                    "name": "",
                    "address":
                      {
                        "address_string": "",
                        "latitude": "",
                        "longitude": ""
                      }
                    "comm":
                      {
                        "email": "",
                        "mobile":""
                      }
                  }
                ]
            }

  /{party_id}
    get:

    put:

# Order
/order
  get:

  post:

  /{order_id}
    get:

    put:

    delete:


# Shipment
/shipment
  get:

  post:

  /{shipment_id}

    get:

    put:

    delete:
